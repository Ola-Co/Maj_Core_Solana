{
  "scripts": {
    "test": "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "deploy:localnet": "CLUSTER=localnet ts-node --transpile-only scripts/deploy.ts",
    "deploy:devnet": "CLUSTER=devnet ts-node --transpile-only scripts/deploy.ts",
    "deploy:mainnet": "CLUSTER=mainnet ts-node --transpile-only scripts/deploy.ts",
    "create-multisig": "ts-node --transpile-only scripts/create_multisig.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.5",
    "@types/chai": "^4.3.12",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.14.0",
    "chai": "^4.3.4",
    "mocha": "^10.4.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
